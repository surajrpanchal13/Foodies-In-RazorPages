@page
@model Taste.Pages.Admin.Category.IndexModel
@{
    ViewData["Title"] = "Food List"; 
}

<div class="border backgroundWhite container">
    <div class="row">
        <div class="col-6">
            <h2 class="text-primary">Food List</h2>
        </div>
        <div class="col-6 text-right">
            <a asp-page="Upsert" class="btn btn-primary"><i class="fas fa-plus"></i> &nbsp; Create New</a>
        </div>
    </div>
    <br /><br />
    <table id="dt-foodType" class="table table-striped table-bordered" style="width:100%">
        <thead>
            <tr>
                <th>Food Type</th>
                <th></th>
            </tr>
        </thead>
    </table>
</div>

@section Scripts{
    <script src="~/js/foodtype.js"></script>
}
